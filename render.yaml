services:
  - type: web
    name: avidus-taskflow-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/app.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CLIENT_URL
        value: https://taskflow-frontend-pi-mocha.vercel.app
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
